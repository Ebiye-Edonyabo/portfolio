<div class="bg-black border-gray-600 border p-4 md:p-6 rounded-lg">
    <div class="flex items-center justify-center gap-3 w-full h-full">
        <h3 class="font-medium text-sm text-white">
            <img src="{{ $asset }}" alt="{{ $title }}" class=" w-12 h-12 object-cover">
            <span class="text-primary-300 font-bold">{{ $title }}</span>
        </h3>
    </div>
</div>